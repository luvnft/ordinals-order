import { meta } from '@/lib/metadata';
import { VerifyForm } from './verify-form';

export default function VerifyPage() {
  return <VerifyForm />;
}

export const metadata = meta({ subtitle: 'Verify' });
